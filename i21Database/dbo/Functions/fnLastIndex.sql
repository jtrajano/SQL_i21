CREATE FUNCTION dbo.fnLastIndex(@STRING VARCHAR(8000), @CHAR CHAR)
RETURNS INT
AS
BEGIN
	IF ISNULL(@STRING, '') = '' OR @CHAR = NULL
	RETURN 0
	DECLARE @pos int
	select @pos = LEN(@STRING) - CHARINDEX(@CHAR, REVERSE(@STRING)) + 1
	IF @pos > len(@STRING)
	SELECT @pos = 0
RETURN @pos
END